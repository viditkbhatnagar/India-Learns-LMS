# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
out/
.vite/
*.tsbuildinfo

# env (only .env.example is committed)
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# coverage
coverage/
*.lcov

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# misc
.cache/
.tsbuildinfo

# Entrance-exam plaintext credentials (generated by seed:entrance; distribute
# locally, never commit).
entrance-credentials.csv

# Playwright + Lighthouse artefacts (generated per-run; not committed)
web/test-results/
web/playwright-report/
docs/screenshots/
lighthouse-student.json
lighthouse*.report.*

# Raw .docx/.pptx exports from the curriculum generator. Local reference
# only — the import contract lives in curriculum-import/SCHEMA-NOTES.md and
# the JSON fixtures, not these rendered exports.
curriculum_generated_outputs/

# Showcase collateral (company profile + program brochures). Large PDFs
# (~120 MB total) — ingested into GridFS via `npm run seed:showcase -w api`,
# never committed. Keep them locally; point the seed at them with
# SHOWCASE_DIR if they live elsewhere.
INDIA LEARNS PROFILE FINALLLL.pdf
INDIA LEARNS DIPLOMA IN FASHION DESIGN & RETAIL MANAGEMENT.pdf
DIPLOMA IN DIGITAL FASHION ENTREPRENEURSHIP.pdf
